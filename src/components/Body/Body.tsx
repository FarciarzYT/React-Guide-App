import React from "react";
import Main from "./Main"; // Załóżmy, że Main jest już zdefiniowany, jak pokazano wcześniej

const Body = () => {
  return (
    <div className="flex h-screen">
      <Main />
    </div>
  );
};

export default Body;
